# @namewta/web-domain-operations

Vue pages and manifest registrations for the `operations` domain. Host-specific request, permission, dictionaries, modal, download, iframe, and public monitor URL values enter through `OperationsWebRuntime`; URL trust decisions remain in `@namewta/domain-operations`.