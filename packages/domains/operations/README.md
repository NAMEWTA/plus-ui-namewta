# @namewta/domain-operations

Headless monitor and operations contracts. The package owns transport paths, models, path-segment encoding, permission-aware external URL validation, and immutable navigation intents. It must not import Vue, DOM APIs, browser storage, or root `src` modules.