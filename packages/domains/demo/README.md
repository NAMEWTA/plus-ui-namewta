# 示例业务领域

`@namewta/domain-demo` 已激活，供 Admin 与 Client Web 共同使用。它拥有普通列表、树表等示例业务的终端无关模型、查询参数、命令和传输映射。

本领域不拥有 Vue 页面、工具栏、分页组件、App 路由、全局请求实例或布局。HTTP 与宿主反馈通过端口注入。

后端映射为 `ruoyi-demo`。验证覆盖列表/树结构映射、查询与变更负载、错误处理和双 App 复用。
