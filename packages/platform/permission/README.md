# 权限平台合同

`@namewta/platform-permission` 提供统一的角色、权限标识和访问评估合同，供指令、命令式检查与领域页面复用。

本包只决定前端可见性与可操作性，不替代后端鉴权。缺少会话、空输入、未知角色或不满足权限时必须失败关闭。

不得依赖 Vue 指令、App Store、后端实现或浏览器全局；这些集成由消费方完成。验证覆盖角色、权限、超管语义和边界输入的一致性。
