export type { ResetPwdForm, UserProfileForm, UserProfileInfoVO } from './types';

export const systemProfileResource = Object.freeze({
  controller: 'SysProfileController',
  basePath: '/system/user/profile'
});
