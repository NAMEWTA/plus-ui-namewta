# Client Web 应用占位

本目录当前仅保留规划占位，不是 pnpm workspace 包，不包含源码、环境变量、依赖、构建脚本或部署产物。

未来重新激活前，必须通过独立规格明确产品范围、ClientId 与登录域、所选 domain/web-domain、会话命名空间、布局与品牌、终端适配、安全合同、测试和部署方式。未经规格确认，不得恢复旧实现或复制 `admin-web`。

激活时应从 `packages/domains/*` 和适用的表现层公开入口组合能力；App 只拥有终端入口、布局、品牌、主题、路由与部署配置，不重复实现后端 API、领域类型或共享业务规则。
